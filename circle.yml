machine:
  environment:

dependencies:
  override:
    - "curl -s -H \"Authorization: token ${GITHUB_AUTH_TOKEN}\" https://raw.githubusercontent.com/infusionsoft/circle-ci-docker-scripts/master/init.sh | bash"

test:
  override:
    # - ./gradlew test
    # - mv build/test-results $CIRCLE_TEST_REPORTS/junit


deployment:
  production:
    branch: master
    commands:
      - ~/scripts/docker-build.sh infusionsoft/${CIRCLE_PROJECT_REPONAME}:$(git tag --contains ${CIRCLE_SHA1})
#      - deploy ${CIRCLE_SHA1} production
